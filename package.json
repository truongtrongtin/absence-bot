{
  "name": "absence-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "test": "vitest"
  },
  "dependencies": {
    "chrono-node": "^2.7.5",
    "itty-router": "^5.0.17",
    "slack-edge": "^0.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "3.57.1"
  }
}
